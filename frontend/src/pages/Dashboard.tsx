import { Link } from "react-router-dom";
export function Dashboard({ wallet }: { wallet?: string }) { return <div><h1 className="text-3xl font-black">Dashboard</h1><div className="mt-8 grid gap-4 sm:grid-cols-4">{[["Total products", "Query registry"], ["Active products", "On-chain status"], ["Invalidated", "On-chain status"], ["Connected wallet", wallet ? `${wallet.slice(0, 6)}…${wallet.slice(-4)}` : "Not connected"]].map(([a,b]) => <div className="rounded-xl border border-stone-200 p-5" key={a}><p className="text-sm text-stone-500">{a}</p><p className="mt-2 font-bold">{b}</p></div>)}</div><div className="mt-10 rounded-xl bg-sand p-6"><h2 className="font-bold">My products</h2><p className="mt-2 text-sm text-stone-600">ERC-721 enumeration is intentionally not included in this lean contract. Use a wallet/indexer to discover owned tokens, then view their records here.</p><Link to="/verify/SG-JKT-000001" className="mt-4 inline-block text-sm font-semibold text-accent">Try a verification lookup →</Link></div></div> }
